# 2026-08-20 — Флаг: 01_parsing_nspd_v8.py перестал парсить НСПД

## Задача
Пользователь сообщил: парсер контуров (`01_parsing_nspd_v8.py`)
перестал работать — не парсит НСПД, видимо изменилась логика сайта.
Просьба: пометить проблему, в будущем поискать решения на форумах/
GitHub, доработать. Не просили чинить сейчас.

## Что сделано
Не диагностировал и не чинил — нет доступа к nspd.gov.ru в среде
разработки этой сессии, реальная причина неизвестна (могла измениться
разметка m-* веб-компонентов, WFS/PKK endpoints, URL-схема карты — что
угодно). Только пометил проблему там, где её увидит следующий, кто
откроет файл:
- `parser/scripts/01_parsing_nspd_v8.py` — предупреждение в самом верху
  docstring, до истории версий.
- `parser/scripts/README_NSPD_CONTOURS.md` — баннер сразу под
  заголовком.

TODO на будущее (зафиксировано в обоих местах): поискать актуальные
обходные пути на форумах кадастровых инженеров/CADASTRE.RU/issues в
открытых ЕГРН/НСПД-парсерах на GitHub, доработать под новую разметку.
Шаги 2-3 конвейера (`01b_ingest_contours.py`,
`kml_apply_nspd_contours.py`) не затронуты — проблема только в сборе
данных с самого НСПД.

## Канал доставки
git push на `claude/ekcelofotomobile-folders-packages-4tryj0`.
