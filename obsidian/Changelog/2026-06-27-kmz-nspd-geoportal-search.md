# 2026-06-27 — KMZ/NSPD: переход на geoportal-search (контур наконец парсится)

## Симптом
`kmz --nspd` ловил **0 feature**, хотя карта NSPD **показывала** контур после
клика по лупе («вывелся объект с контуром и описанием, но не спарсилось»).

## Диагноз (по сохранённой странице заказчика)
Страница = Vite module-federation SPA; сам map-бандл и XHR-ответ с геометрией в
«Save as» **не попали**. Но улика нашлась в логике v8: `NetworkCapture`
**пропускает любой URL c `/search/`** (правило «search = extent квартала» — из
эпохи ПКК). Современный NSPD отдаёт геометрию объекта именно эндпоинтом
`https://nspd.gov.ru/api/geoportal/v2/search/geoportal` (его и дёргает лупа) →
реальный ответ молча отбрасывался.

## Фикс — `geo_nspd_browser.py` переписан
- **Активный** session-aware запрос через `page.request.get` к geoportal-search
  (наследует cookies/Referer открытой карты → проходит анти-бот там, где голый
  urllib даёт 403). Варианты: `?query=КН` и `?thematicSearchId=1&query=КН`.
- Парс ответа терпим к обёрткам (`{data:{features}}` / FeatureCollection /
  Feature); КН берётся в т.ч. из `properties.options.cad_num`.
- Геометрия в EPSG:3857 → репроекция в WGS84 (inline, формула из v8).
- Доп. **разрешающий** слушатель ответов (НЕ режет `/search/geoportal`) копит
  feature карты → обнаружение ОКС в границах ЗУ (centroid-in-polygon) + fallback.
- Контракт `fetch_parcels` неизменен: `{cad:{polygon,buildings,captured}}`.

## CLI
- Диагностика переименована: «feature от geoportal», подсказка `--nspd-headful`.

## Тесты
- Новый `tests/test_geo_nspd_browser.py` (+7): extract/обёртки, репроекция
  3857→WGS84 (даёт Краснодарский край для 23:15), выбор ЗУ по точному КН,
  КН из вложенного `options`, обнаружение ОКС. **Итого geo: 24 passed.**
- Браузерный `_run` проверяется на машине заказчика (нужны playwright+chromium+сеть).

## Версия 2: закрытие модалей и ретрай
После 1-го тестирования (`геометрия по 1/3`) добавлена логика:
- `_close_modals(page)` — закрывает видимые диалоги (X, overlay, ESC).
- Увеличена задержка после page.goto (2.5s + close + 1s) вместо 1.5s.
- `_search_geoportal` повторяет запрос до 3 раз с задержками.

## Версия 3+4: контуры ЗУ работают (3/3), добавлен клик по лупе для ОКС
- v3: `wait 3s` после geoportal-search — ОКС всё равно 0.
- Диагноз заказчика: NSPD предзаполняет КН в строке поиска, но **тянет данные
  (контур + ОКС в пределах) только после клика по лупе**. Прямой `page.request`
  отдаёт контур ЗУ (поэтому 3/3), но ОКС грузит лишь UI-поиск.
- v4: `_trigger_lupa(page, cad)` — рекурсивно по OPEN shadow-root находит поле
  ввода и кнопку-лупу (web-components NSPD), заполняет КН, шлёт Enter + клик.
  Карта грузит ОКС → пассивный `_on_resp` их ловит → фильтр centroid-in-polygon.
  Ожидание после клика поднято до 4s.

## Версия 5: ручной режим `--nspd-manual` (человек в цикле)
Авто-клик лупы ОКС не вытянул (geoportal-search отдаёт только контур ЗУ; данные
вкладки «ОКС в пределах» NSPD грузит лишь при реальном взаимодействии). Решение
по идее заказчика — **человек в цикле**:
- `--nspd-manual` → принудительно видимый браузер; по каждому КН скрипт открывает
  страницу и **ждёт Enter в консоли**, пока пользователь жмёт лупу и листает
  вкладки (ОКС в пределах ЗУ).
- Ожидание Enter — через `loop.run_in_executor(None, input)` → event-loop НЕ
  блокируется, пассивный слушатель продолжает копить features во время кликов.
- Слушатель в ручном режиме ловит **любой** JSON c nspd (не только /geoportal/),
  т.к. данные вкладок идут с других путей; `extract_features` отсеет не-feature.
- После Enter — парсим перехваченное, ОКС фильтруем centroid-in-polygon, идём к
  следующему КН.

## Проверка на машине заказчика
Авто (контуры ЗУ, работает 3/3):
```
python -m egrn_parser kmz --parcels 23:15:0000000:2267,23:15:0303000:1562,23:15:0303000:1130 \
  --db проект.sqlite --out objects.kmz --nspd --nspd-headful
```
Ручной (контуры + ОКС, человек в цикле):
```
python -m egrn_parser kmz --parcels 23:15:0000000:2267,23:15:0303000:1562,23:15:0303000:1130 \
  --db проект.sqlite --out objects.kmz --nspd-manual
```
По каждому КН: нажать лупу, пролистать вкладки (ОКС в пределах ЗУ) → Enter в консоли.

## Версия 6: вкладка ОКС = таблица КН (без геометрии) → резолв через search
Прогон `--nspd-manual` дал «ОКС найдено: 0», captured=4 (клики не добавили
feature). Вывод: вкладка «ОКС в пределах ЗУ» — **атрибутивная таблица КН без
геометрии**, поэтому `extract_features` ничего не находит.
Фикс:
- `collect_cads(payload)` — рег-выражением `\d2:\d:\d6-7:\d` собирает ВСЕ КН из
  любого ответа (вкл. таблицы вкладок).
- В ручном режиме после Enter: берём КН-кандидаты ОКС (минус сам ЗУ) и
  **резолвим геометрию каждого проверенным geoportal-search** (он работает для
  ЗУ → работает и для ОКС). Затем centroid-in-polygon.
- Диагностика: печать всех JSON-ответов NSPD (url/keys/feats/cads) — если ОКС
  снова 0, видно, что именно отдаёт портал.
- Тесты +2 (`collect_cads`): **geo 26 passed**.

## Файлы
- `parser/egrn_parser/geo_nspd_browser.py` (переписан),
  `parser/egrn_parser/cli.py` (диагностика),
  `parser/tests/test_geo_nspd_browser.py` (новый).
