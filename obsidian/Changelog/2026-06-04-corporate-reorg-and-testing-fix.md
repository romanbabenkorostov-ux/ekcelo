# 2026-06-04 — Руководитель + реорганизация (corporate) + фикс теста экономиста

## Суть
По запросу заказчика: (1.1) руководитель ЕГРЮЛ — субъект; (1.2) реорганизация/
правопреемство в схеме и описании; (2) починена инструкция тестирования (тест упал
из-за отсутствующей БД парсера).

## Сделано
### Корпоративные связи (1.1, 1.2)
- `relation_types_seed.py`: +`SUCCESSOR_OF`, +`REORGANIZING_WITH` (categ. `corporate`);
  `MANAGES` уточнён («без доверенности»); добавлен `REORG_TYPES`
  {merger,affiliation,division,spin_off,transformation}. Итого **32 кода**.
- `DOC_CLASSIFIER_SPEC.md §3.2 (ЕГРЮЛ)`: руководитель («лицо без доверенности», ФЛ/ЮЛ)
  → отдельный subject + `MANAGES`; правопреемник → `SUCCESSOR_OF` (meta.reorg_type);
  незавершённая реорг. → `REORGANIZING_WITH`. Основание ФЗ-129 ст.5 п.1 пп «ж».
- `ekcelo-system-description.md §4`: руководитель как субъект; новые §4.1 (таблица
  corporate-рёбер) и §4.2 (реорганизация: 5 видов, правопреемник≠учредитель,
  незавершённая реорг.).
- `SCHEMA_SPEC.md §2.2`: коды corporate расширены.

### Фикс теста (2)
- `make_demo_block2.py` — генератор демо Block-2 БД (проверка C2 без парсера).
- `db-testing-economist.md` переписан на 2 трека: **A** (демо, без парсера) и
  **B** (реальные ЕГРН через `egrn-parser parse --input <EGRN> --db output\egrn.db`).
  Объяснена прошлая ошибка (указан PDF вместо БД; ЕГРЮЛ не подаётся в import_block2).

## Проверено (SQLite)
Демо-чейн make→import→emit зелёный; relation_types в БД = 32 (corporate 6);
`pytest contracts/db/tests` — 2 passed.

## Не требует миграции
`SUCCESSOR_OF/REORGANIZING_WITH` — строки справочника `relation_types` (данные), не схема.

## Дальше
Ingest руководителя/правопреемства — стадия классификатора ЕГРЮЛ (этап 2). В Block-2
БД парсера директор/правопреемник пока не выгружаются — появятся с ЕГРЮЛ-стадией.
