# 2026-05-18 — S4: роли pro/view/embed (viewer)

**Что:** реализована спека `dev/SPEC_ROLES_VIEWER_EMBED.md` в `viewer/index.html`.
Мягкое гейтирование UI (не криптозащита, §2 spec).

**Сделано:**
- Роль-движок: `ROLE_CFG` (DEFAULT_ROLE/PRO_PASS_SHA256/EMBED_CHROME),
  `resolveRole()` (приоритет: ?mode=embed>view>pro > localStorage > DEFAULT),
  `roleAllows(cap)`, `applyRole()` (динамически, без reload, идемпотентно),
  вызов на старте скрипта.
- Гейт-`return` в 12 обработчиках §5 (edit: startContourEditor,
  toggleContourShiftMode, deleteContourShape, moveContourToOtherProject,
  _commitEditedGeometryInPlace, startExplanationPoint, _editContourFromCard,
  _editExpCoordFromCard, writeEXIFWithGPS; export: toggleExportMenu,
  _exportCadastreTiles, exportData).
- CSS `html.role-view/.role-embed` прячет `#export-wrap`; embed — `#header`,
  `#ver-switch`, `#ekcelo-role-lock`. Вне зоны CSS-шва (439–468).
- Pro-разблокировка: элемент `#ekcelo-role-lock`, SHA-256-сверка с
  `ROLE_CFG.PRO_PASS_SHA256`, localStorage `ekcelo_role`, честная подпись.
- embed: нейтрализация FSA-пикеров (delete showDirectoryPicker/SaveFilePicker/
  OpenFilePicker) → существующие `<input>`-fallback'и (§7).

**Инварианты:** `DEFAULT_ROLE:'pro'` → дефолт байт-в-байт прежний
(pro = no-op гейт, §10 spec); `node --check` инлайн-скрипта чист; CSS-шов и
`v2961.html` не тронуты; изменён только `viewer/index.html`.

**Требует владельца (перед мержем):**
1. `ROLE_CFG.PRO_PASS_SHA256` = hex sha256 парольной фразы (сейчас `''` →
   pro-разблокировка выключена). Сгенерировать:
   `node -e "crypto.subtle.digest('SHA-256',new TextEncoder().encode('ФРАЗА')).then(b=>console.log(Buffer.from(b).toString('hex')))"`
2. Подтвердить `DEFAULT_ROLE` ('pro' рекоменд. — не ломает текущих юзеров).
3. Ручная проверка 3 ролей в браузере (§11 spec — в песочнице браузера нет).

**Остаток (полировка, не блокер):** визуальное скрытие динамических
edit-«карандашей» в инфо-карточках (функционально уже обезврежены гейтом).
