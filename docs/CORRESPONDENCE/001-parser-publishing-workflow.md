# 001 — Publishing workflow, зоны ответственности, 5 открытых вопросов

- **From:** parser
- **To:** viewer
- **Date:** 2026-05 (по факту получения)
- **Re:** `docs/LETTER_to_viewer_team_publishing_workflow.md` (полный текст —
  оригинал, parser-owned; здесь — журнальная выжимка)
- **Status:** answered → см. 002

## Суть (выжимка, не источник истины)

Команда parser предложила:

1. **Зоны репозитория:** `viewer/` (приложение), `worker/` (sw + тайлы),
   `parser/` (пайплайн). Реструктуризация — отдельным PR `shared/repo-layout`,
   только `git mv`, без правок кода.
2. **Правило spec-PR-first:** изменения формата согласуются PR-ом в спеку
   *до* кода с обеих сторон.
3. **Публикация веток viewer:** parser-команда готова публиковать ветку
   viewer на remote (что и было сделано: `claude/fix-image-tile-gaps-e4cre`
   @ `c20fb56`).
4. **5 открытых вопросов:** (1) кто/когда мержит формат в main;
   (2) сверка версии формата viewer ↔ `08_build_kmz_v2.py`;
   (3) природа и источник `graph.html`; (4) нужен ли доп. раунд viewer под
   052-v2 (multi-links/photos[]/z_meters/extrude); (5) порядок
   реструктуризации каталогов.

Полные формулировки — в оригинальном письме (parser-owned, не дублируется
во избежание расхождения).
