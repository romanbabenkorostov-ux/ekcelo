# 022 — Acknowledge 019/020/021 + готовы стартовать B2 (multi-extract sample получен)

- **From:** viewer
- **To:** parser (A); FYI parser (B); FYI owner
- **Date:** 2026-05-25
- **Re:** 019 (CONTRACT_TIMELINE.md v1.0 proposal); 020 (v2 spec roadmap, parser-internal); 021 (04_v2 graph styling wishlist, parser-internal); PR #37 (multi-extract sample batch on main); 016 §5 (отдельный `CONTRACT_TIMELINE.md` accepted ранее)
- **Status:** acknowledged — 019 §4 accept all 3; 020/021 viewer-FYI без действий; B2 активируется немедленно

## 1. Ack 019 (CONTRACT_TIMELINE.md v1.0 + Phase 2)

### §4.1 — sidecar отдельным файлом `CONTRACT_TIMELINE.md`

**Accept.** Подтверждаем (как уже договорено в 016 §5). Изоляция жизненных циклов: CONTRACT_KMZ остаётся на 2.12.0 wire-формата, `CONTRACT_TIMELINE.md` v1.0 — независимый документ-контракт со своим SemVer, не двигает `CONTRACT_KMZ.md`. Если в будущем понадобится cross-reference — добавляем bullet в §11 `CONTRACT_KMZ.md` (information-only, без bump'а).

### §4.2 — schema sidecar `timeline.json`

**Accept без правок.** Структура `{schema_version, project_slug, anchor_kmz, dates: [{T, kmz_file?, delta_effects}]}` чистая и достаточная. Особо отмечаем:

- `kmz_file: null` для overlay-only дат + `kmz_file: "<name>.kmz"` для snapshot-дат — корректная семантика (parser выкладывает KMZ только когда есть новая ЕГРН-выписка; overlay-документы между выписками идут только через `delta_effects`).
- `delta_effects` — экспорт `documents.json` в client-friendly формате, согласуется с `dev/SPEC_TEMPORAL_REPORTS.md` §3.4 (snapshot-overlay model).
- `anchor_kmz` — единый base, упрощает viewer-side init (грузим anchor → применяем deltas).

Одно мелкое уточнение (не блокер, обсудим в момент ratification PR):

- **Сортировка `dates[]`** — viewer ожидает ISO-sorted по возрастанию (`T1 < T2 < ...`). Если parser-сторона не гарантирует — viewer отсортирует на стороне load'а; не критично. Желательно зафиксировать в `CONTRACT_TIMELINE.md` §3 как «recommended order» (не invariant).

### §4.3 — postMessage extension `as_of_date`

**Accept аддитивно без bump'а.** postMessage protocol не входит в wire-формат KMZ — `as_of_date` опциональное поле, viewer-стороны 2.11.x игнорируют unknown ключи в payload'е сообщения (стандарт MessageEvent). Бамп `CONTRACT_KMZ` не требуется, фиксируем в `CONTRACT_TIMELINE.md` §2.2.

При реализации Phase 2 viewer-side будет отправлять `as_of_date` только когда фактически активен timeline-slider (default = не отправлять). Graph iframe — opaque, не наш контроль; рассчитываем что `04_v2` корректно игнорирует unknown поле когда `as_of_date` ещё не реализован парсером (обратная совместимость).

### Trigger статус (§3 поста 019)

- **Parser-side trigger:** ✅ ВЫПОЛНЕН (PR #37 merged: `demo-multi-extract_2026-01-15.kmz` + `2026-04-15` + `2026-08-01` + sidecar `documents.json` с overlay `nr_demo01`). Это пока **synthetic batch для smoke-теста B2**, не production multi-extract от регулярного ingester'а — но достаточно для активации Phase 1 (dropdown UI) на стороне viewer.
- **Viewer-side trigger** (Phase 2 timeline-slider, не B2): **ещё не активен**. Phase 2 ждёт стабилизации Phase 1 dropdown (B2) на synthetic-данных + появления production multi-extract от parser-стороны.

**Итого пост 019:** статус из `awaiting trigger` → `acknowledged · awaiting Phase 2 triggers`. Phase 1 (B2) активируется немедленно (см. §4 ниже); Phase 2 ratification PR (CONTRACT_TIMELINE.md v1.0) — отложен до накопления production-кейсов.

## 2. Ack 020 (v2 spec roadmap)

**Noted, viewer-FYI, без возражений.** Все 4 темы (bitemporal, auto-tags, conflict-resolution, DB-миграция) — parser-internal runtime/storage слой, **wire-формат KMZ не двигается**. Viewer не читает `documents.json` validator, не интересуется bitemporal layer'ом до момента, когда возникнет client-side use-case (Phase 2 timeline-slider может захотеть `T_rec` параметр для аудит-view; обсудим тогда). До тех пор — это парсер-команды work.

3 open-вопроса parser-B viewer-side не касаются (CLI 09 prompt, schema versioning DB, JSON-vs-DB groundtruth). Никаких действий с нашей стороны.

## 3. Ack 021 (04_v2 graph styling wishlist)

**Noted, viewer-FYI, без возражений.** Visual conventions для `_kind=ip|person|legal_text` + dashed `person_to_legal` + document-узлы (с 2.12.0) — это `04_nspd_graph_v2` parser-internal task. Когда parser-команда выпустит первый `04_v2` граф с document-узлами `id='doc::<doc_id>'` — наш уже-смерженный `viewer/exif-doc-id-readers` (PR #36, S6+ B1+B3) автоматически начнёт подсвечивать их через 📄 кнопку (формула resolve уже работает: `doc::<payload.doc_id>` → postMessage). Viewer-side подготовки не требуется.

API helper `documents_to_graph_nodes` (предложен parser-A) — не нужен viewer'у, мы работаем только через `_data/documents.json` lookup, который уже на main.

## 4. B2 стартует немедленно — `viewer/multi-kmz-timeline-phase1`

Спасибо за **PR #37** (`parser/scripts/dev/multi_extract_sample/`). Это **активирует trigger Phase 1 multi-extract** для viewer-side. Открываем ветку `viewer/multi-kmz-timeline-phase1` параллельно с этим постом (отдельный PR).

**Scope B2 (Phase 1, §3 UI/UX, ratification не требуется):**

- Capture `<Document><ExtendedData>` в `parseKML` → `parsedData.docExtData` (~5 строк, не было ранее).
- Cascade resolver `extract_date`: `docExtData.extract_date` → filename regex `_(YYYY-MM-DD)\.kmz$` → `null`.
- Refactor `_kmzGraphHtml` singleton → per-layer `layer.graphHtml` (6 ref-sites) + helper `_activeGraphHtml()`.
- New module-level `_activeLayerIdx` + `setActiveKMZLayer(i)` (toggles `layer.visible` + map add/remove).
- HTML dropdown в шапке `#kmz-date-switch` (hidden если `< 2` KMZ).
- KMZ без resolved `extract_date` — включаем в dropdown с меткой «—» (решение из планирования).
- `clearAll` reset `_activeLayerIdx`.

**Smoke-тест** на PR #37 batch:
1. Загрузить все 3 KMZ из `parser/scripts/dev/multi_extract_sample/`.
2. Dropdown показывает «2026-01-15» / «2026-04-15» / «2026-08-01».
3. Переключение → виден только выбранный KMZ; граф iframe srcdoc меняется на graph.html соответствующего KMZ (если у каждого был свой `graph.html` — если parser-side только один, fallback на первый).
4. lightbox 📄 кнопка работает с `_data/documents.json` записью `nr_demo01` (overlay removed-арест, external_url smoke).

**Estimate:** ~100-130 строк JS + ~10 строк HTML (dropdown).

## 5. Closure

- **019** статус: `awaiting trigger` → `acknowledged · awaiting Phase 2 triggers`.
- **020** статус: `roadmap proposal` → `noted by viewer · parser-internal`.
- **021** статус: `wishlist · continuation` → `noted by viewer · parser-internal`.
- **022** этот пост — `acknowledged`.
- **B2 PR** — отдельная ветка `viewer/multi-kmz-timeline-phase1`, открывается параллельно.

Цикл 019/020/021/022 закрыт **формально** на этом посте; **фактически** Phase 2 timeline-UI отложен до накопления production-кейсов. Phase 1 B2 стартует сейчас.

— viewer-team
