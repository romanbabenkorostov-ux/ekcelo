# EkceloFoto — Чеклисты

## A. Регрессия шва тайлов (ОБЯЗАТЕЛЬНО при любом касании `index.html`)

Хрупкий фикс — 5 CSS-правил `index.html` ~457–468 (защита-комментарий ~400–456).
История: 7 провальных попыток v2.9.30–v2.9.50; рабочее с v2.9.53.
**Любое изменение рядом с этим блоком требует пройти весь список:**

- [ ] Chrome — нет белой сетки между тайлами
- [ ] Firefox — нет белой сетки
- [ ] Яндекс.Браузер — нет белой сетки
- [ ] zoom 10 (обычные тайлы)
- [ ] zoom 18 (нативные)
- [ ] zoom 22 (апскейл за maxNativeZoom)
- [ ] HiDPI-монитор (devicePixelRatio > 1)
- [ ] Тёмная тема (--map-bg)
- [ ] Светлая тема (фон #f2efe9 — на нём щели заметнее всего)
- [ ] Маркеры фото, контуры, фото-пины рендерятся корректно
- [ ] Сверка с замороженной `v2961.html` (баннер сверху) — поведение идентично

## B. PR-чеклист (каждый коммит)

- [ ] Версия обновлена: `index.html` `<title>` (стр.10) и `<small>` (логотип),
      `sw.js` шапка-комментарий
- [ ] Ветка только `claude/fix-image-tile-gaps-e4cre` (push в неё)
- [ ] `sw.js CACHE_NAME` НЕ изменён (иначе вайп кэша пользователя)
- [ ] Коммит затрагивает `index.html` (иначе деплой на `main` не триггернётся)
- [ ] Стиль дома не переписан массово
- [ ] `bash dev/lint.sh` — нет `no-undef` / `no-redeclare` в новом коде
- [ ] `dev/tests` — не-`@network` smoke зелёные
- [ ] Чеклист A пройден (если трогали `index.html`)
- [ ] `v2961.html` = чистая 2.9.61 + только баннер (diff подтверждён)

## C. Обмен тайлами (46c) — приёмка

- [ ] Экспорт «Выгрузить тайлы Росреестра z17» → ZIP с `tiles/*.png` + `manifest.json`
- [ ] Импорт того же ZIP в чистый кэш → «Добавлено N, пропущено 0»
- [ ] Повторный импорт → «Добавлено 0, пропущено N» (идемпотентно/обогащение)
- [ ] Бейдж `#cadastre-cache-info` растёт; тайлы рендерятся из кэша оффлайн
