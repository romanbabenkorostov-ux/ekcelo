"""
egrn_parser GUI — графическая оболочка разбора выписок ЕГРН (PySide6).

Пакет намеренно отделён от `egrn_parser/`: вся логика прохода лежит в
`egrn_parser.geo_pipeline`, здесь только окно. Так парсер остаётся
запускаемым из консоли и из тестов на машине, где Qt не установлен.
"""
